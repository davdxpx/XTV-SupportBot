apiVersion: v1
kind: Service
metadata:
  name: {{ include "xtv-support.fullname" . }}
  labels: {{- include "xtv-support.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector: {{- include "xtv-support.selectorLabels" . | nindent 4 }}
