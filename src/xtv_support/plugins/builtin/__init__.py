"""First-party plugins bundled with the distribution.

Each subfolder is one plugin. They are discovered automatically and gated
behind feature flags so the bot still boots when an optional dependency
extra is not installed.
"""
