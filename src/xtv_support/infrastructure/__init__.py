"""Adapters to external systems — databases, cache, storage, Telegram, AI.

Every module here is an implementation detail. Services depend on the
abstractions declared next to them, not on concrete adapters.
"""
