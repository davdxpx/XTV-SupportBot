"""Kernel building blocks: DI container, event bus, router, logger, FSM.

Existing kernel modules (router, filters, logger, errors, callback_data,
context) land here via `git mv` in Phase 2b. Container / events / state
are introduced in Phase 3.
"""
