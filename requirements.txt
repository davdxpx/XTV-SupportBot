pyrofork>=2.3.60
tgcrypto>=1.2.5
motor>=3.5.0
pymongo>=4.8.0
dnspython>=2.6.1
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1
pyyaml>=6.0.1
structlog>=24.1.0
# REST API + admin SPA server. Kept in the base requirements so
# Railway/Heroku/nixpacks builds can expose /health + /api/v1/* without
# extra install flags; the app itself only starts when API_ENABLED=true.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
# Async HTTP client — used for Bot-API side calls like setChatMenuButton
# that don't go through pyrofork's MTProto stack.
httpx>=0.27.0
